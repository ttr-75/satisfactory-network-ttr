require("config.lua")

local Helper_log = require("shared/helper_log.lua")
local log = Helper_log.log


log(4, "You have not set the name of the starter File, so I've started test/test.lua by default.")
